# Epic 3: Gamification & Polish

- `[ ]` **Schema Update**: Add `xp`, `current_streak`, `last_debate_date` to `profiles`.
- `[ ]` **XP Logic**: Update `updateSkills.ts` to grant XP upon debate completion and check streak logic.
- `[ ]` **Dashboard UI**: Display Streak and XP bar.
- `[ ]` **Motion of the Day**: Display a daily challenge motion.
- `[ ]` **Debate Specifics**: Add "Flow Sheet" digital notepad to the Record page.
- `[ ]` **Debate Specifics**: Add a 15-minute Prep Timer before allowing recording.
- `[ ]` **Animations**: Add `framer-motion` page transitions or `react-loading-skeleton` where necessary.
